---
-
  clean:
      - "~"
-
  link:
      ~/.fonts: github/powerline-fonts
      ~/.gitconfig: gitconfig
      ~/.tmux.conf: tmux.conf
      ~/.vim: vim/vim
      ~/.vimrc: vim/vimrc
      ~/.zshrc: zshrc
