---
-
  clean:
      - "~"
-
  link:
      ~/.fonts: github/powerline-fonts
      ~/.gitconfig: gitconfig
      ~/.tmux.conf: tmux.conf
      ~/.vim: vim/vim
      ~/.vimrc: vim/vimrc
      ~/.zshrc: zshrc
      ~/.kde/share/apps/konsole/Shell.profile:
       create: true
       path: kde/apps/konsole/Shell.profile
-
  shell:
       -
         - "gcc -O3 -o bin/powerline github/powerline/client/powerline.c"
         - "Compiling powerline client"
       -
         - "github/YouCompleteMe/install.sh"
         - "Compiling YouCompleteMe client"
