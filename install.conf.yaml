---
-
  clean:
      - "~"
-
  link:
      ~/.fonts: github/powerline-fonts
      ~/.gitconfig: gitconfig
      ~/.tmux.conf: tmux.conf
      ~/.vim: vim/vim
      ~/.vimrc: vim/vimrc
      ~/.zshrc: zshrc
      ~/.kde/share/apps/konsole/Shell.profile:
       create: true
       path: kde/apps/konsole/Shell.profile
